#!/usr/bin/env bash
# Multi-Agent Development Orchestrator (`mag`) launcher

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${SCRIPT_DIR}:${PYTHONPATH}"

exec python3 -m project.src.cli "$@"
